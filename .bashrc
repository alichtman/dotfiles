# .bashrc
# Aaron Lichtman

###
# Warning: I use zsh and this .bashrc is woefully incomplete. It works, but it doesn't do much.
###


eval $(thefuck --alias)

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
